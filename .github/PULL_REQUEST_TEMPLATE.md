# [Type] Titre concis

## Résumé
Qu'est-ce que cette PR change, en une phrase.

## Lien issue
Fixes #N

## Changements
- [ ] Fonctionnalité / correction décrite

## Vérifications effectuées
- [ ] `npm test` (API + tokens) : vert
- [ ] `npm run test:e2e -w apps/web` : vert (golden images à jour si UI modifiée)
- [ ] `npm run build` : exit 0
- [ ] `grep -r "—" apps/ packages/` : 0 résultat
- [ ] Contrats API intacts (aucun breaking change non documenté)

## Captures (si UI)
Avant / après si pertinent.
